name: SPLRAD Steward / PR Issue Link

on:
  workflow_dispatch:

permissions: {}

jobs:
  pending:
    runs-on: ubuntu-latest
    steps:
      - name: 等待后续堆叠层
        run: echo "PR与议题自动关联将在后续堆叠PR中启用。"
